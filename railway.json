{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve -s frontend/dist -l 8080",
    "restartPolicyType": "ON_FAILURE"
  }
}